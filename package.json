{
  "name": "saturn-system",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "check": "node --experimental-strip-types src/orbital/kepler.selfcheck.ts",
    "deploy": "node scripts/deploy.mjs"
  },
  "dependencies": {
    "three": "^0.178.0"
  },
  "devDependencies": {
    "@types/three": "^0.178.0",
    "typescript": "^5.5.0",
    "vite": "^6.0.0"
  }
}
